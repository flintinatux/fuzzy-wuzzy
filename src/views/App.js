import React from 'react'

import {
  app,
  modal,
  title
} from './App.module.scss'

const App = () =>
  <div className={app}>
    <header>
      <h1 className={title}>Fuzzy wuzzy!</h1>
    </header>

    <section className={modal}>

    </section>
  </div>

export default App
